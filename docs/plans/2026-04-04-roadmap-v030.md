# Roadmap v0.3.0 -> v1.0.1 Implementation Plan

**Statut** : Implemente (v1.0.1)

**Goal:** 10 items UX/features/bugfix issus du retour utilisateur Windows.

**Architecture:** 4 phases d'execution. Phase 1 = quick wins parallelisables. Phase 2 = fondations (layout + zoom + abort IA). Phase 3 = features moyennes. Phase 4 = feature complexe (ignorer doc). Chaque task est un commit.

**Tech Stack:** Electron 39, React 19, TypeScript, Zustand, pdf-lib, pdfjs-dist v4, Tailwind v4, shadcn/ui, react-resizable-panels (nouveau), @anthropic-ai/sdk

---

## Phase 1 -- Quick wins (parallelisables)

### Task 1: Comptes 000000 + 471000 dans plan comptable

**Files:**
- Modify: `src/renderer/src/data/planComptable.ts`

**Step 1:** Ajouter au debut du `csvData` (apres la ligne `const csvData = \`...`) :

```
000000,Documents divers
```

**Step 2:** Verifier que `471000,Comptes d'attente` existe deja dans le CSV. S'il n'existe pas, l'ajouter dans la section 4xx.

**Step 3:** Run `npm run typecheck`

**Step 4:** Commit
```bash
git add src/renderer/src/data/planComptable.ts
git commit -m "feat: add 000000 Documents divers + 471000 Comptes d'attente"
```

---

### Task 2: Tampon plus gros

**Files:**
- Modify: `src/main/services/stampService.ts:58` (stampSingle)
- Modify: `src/main/services/stampService.ts:124` (stampMultiple)
- Modify: `src/renderer/src/components/PdfPreview.tsx` (drawStamp, 2 occurrences du clamp)

**Step 1:** Dans `stampSingle` ligne 58, changer :
```typescript
// Avant
const fontSize = Math.max(8, Math.min(12, pageWidth / 30))
// Apres
const fontSize = Math.max(10, Math.min(16, pageWidth / 22))
```

**Step 2:** Dans `stampMultiple` ligne 124, changer le clamp de la meme maniere. Attention : garder le min a 7pt pour le multi-lignes :
```typescript
const fontSize = Math.max(7, Math.min(16, Math.min(pageWidth / 22, (pageHeight * 0.4) / (n * 1.6))))
```

**Step 3:** Dans `PdfPreview.tsx`, trouver les 2 occurrences de `pdfW / 30` dans `drawStamp` et les remplacer par `pdfW / 22`. Trouver `Math.max(8, Math.min(12,` et remplacer par `Math.max(10, Math.min(16,`. Idem pour le mode ventile (chercher `Math.max(7,`).

**Step 4:** Run `npm run typecheck`

**Step 5:** Commit
```bash
git add src/main/services/stampService.ts src/renderer/src/components/PdfPreview.tsx
git commit -m "feat: increase default stamp size (clamp 10-16, pageWidth/22)"
```

---

### Task 3: ErrorBoundary + diagnostic crash Windows

**Files:**
- Create: `src/renderer/src/components/ErrorBoundary.tsx`
- Modify: `src/renderer/src/main.tsx`

**Step 1:** Creer `ErrorBoundary.tsx` :
```typescript
import { Component, type ReactNode, type ErrorInfo } from 'react'

interface Props {
  children: ReactNode
}

interface State {
  hasError: boolean
  error: Error | null
}

export class ErrorBoundary extends Component<Props, State> {
  constructor(props: Props) {
    super(props)
    this.state = { hasError: false, error: null }
  }

  static getDerivedStateFromError(error: Error): State {
    return { hasError: true, error }
  }

  componentDidCatch(error: Error, info: ErrorInfo): void {
    console.error('[ErrorBoundary]', error, info.componentStack)
  }

  render(): ReactNode {
    if (this.state.hasError) {
      return (
        <div className="flex h-screen items-center justify-center bg-background p-8">
          <div className="max-w-md space-y-4 text-center">
            <h1 className="text-lg font-semibold text-foreground">Erreur inattendue</h1>
            <p className="text-sm text-muted-foreground">{this.state.error?.message}</p>
            <pre className="text-xs text-muted-foreground bg-muted p-3 rounded overflow-auto max-h-40 text-left">
              {this.state.error?.stack}
            </pre>
            <button
              onClick={() => window.location.reload()}
              className="px-4 py-2 text-sm bg-primary text-primary-foreground rounded-md hover:bg-primary/90"
            >
              Recharger l'application
            </button>
          </div>
        </div>
      )
    }
    return this.props.children
  }
}
```

**Step 2:** Modifier `main.tsx` :
```typescript
import './assets/main.css'
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { ErrorBoundary } from '@/components/ErrorBoundary'
import App from './App'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <ErrorBoundary>
      <App />
    </ErrorBoundary>
  </StrictMode>
)
```

**Step 3:** Run `npm run typecheck`

**Step 4:** Commit
```bash
git add src/renderer/src/components/ErrorBoundary.tsx src/renderer/src/main.tsx
git commit -m "feat: add ErrorBoundary to catch and display React crashes"
```

---

## Phase 2 -- Fondations (sequentielles)

### Task 4: Panneaux redimensionnables (react-resizable-panels)

**Files:**
- Modify: `package.json` (nouvelle dep)
- Modify: `src/renderer/src/components/Layout.tsx`
- Modify: `src/renderer/src/components/PdfPreview.tsx` (ResizeObserver pour re-render)

**Step 1:** Installer la dependance :
```bash
npm install react-resizable-panels
```

**Step 2:** Remplacer le contenu de `Layout.tsx` :
```typescript
import { PanelGroup, Panel, PanelResizeHandle } from 'react-resizable-panels'
import { PdfPreview } from './PdfPreview'
import { ComptaForm } from './ComptaForm'
import { FileQueue } from './FileQueue'
import { FileSidebar } from './FileSidebar'
import { ThemeToggle } from './ThemeToggle'

interface LayoutProps {
  onOpenSettings: () => void
}

export function Layout({ onOpenSettings }: LayoutProps): React.JSX.Element {
  return (
    <div className="flex flex-col h-screen relative">
      <FileQueue onOpenSettings={onOpenSettings} />
      <PanelGroup direction="horizontal" className="flex-1 min-h-0">
        <Panel defaultSize={14} minSize={10} maxSize={22}>
          <FileSidebar />
        </Panel>
        <PanelResizeHandle className="w-1 bg-border/40 hover:bg-primary/60 transition-colors duration-150 cursor-col-resize" />
        <Panel defaultSize={52} minSize={35} maxSize={65}>
          <div className="h-full p-4 overflow-auto">
            <PdfPreview />
          </div>
        </Panel>
        <PanelResizeHandle className="w-1 bg-border/40 hover:bg-primary/60 transition-colors duration-150 cursor-col-resize" />
        <Panel defaultSize={34} minSize={28} maxSize={45}>
          <div className="h-full p-4 overflow-auto">
            <ComptaForm />
          </div>
        </Panel>
      </PanelGroup>
      <div className="absolute bottom-2 left-2 z-10">
        <ThemeToggle />
      </div>
    </div>
  )
}
```

**Step 3:** Dans `PdfPreview.tsx`, ajouter un `ResizeObserver` pour re-render le canvas quand le panneau change de taille. Apres les refs existantes, ajouter :
```typescript
// Re-render when container resizes (panel drag)
const containerRef = useRef<HTMLDivElement>(null)
useEffect(() => {
  const el = containerRef.current
  if (!el) return
  const observer = new ResizeObserver(() => {
    if (pdfDoc && currentPage > 0) {
      renderPage(pdfDoc, currentPage)
    }
  })
  observer.observe(el)
  return () => observer.disconnect()
}, [pdfDoc, currentPage, renderPage])
```
Et wrapper le div du scroll container avec cette ref :
```tsx
<div ref={containerRef}>
  <div ref={scrollContainerRef} className="flex-1 overflow-auto flex justify-center items-start">
    ...
  </div>
</div>
```

**Step 4:** Supprimer `w-48 min-w-48` de `FileSidebar.tsx` (le Panel gere la taille) -- remplacer par `h-full overflow-auto bg-card/50`.

**Step 5:** Run `npm run typecheck`

**Step 6:** Commit
```bash
git add package.json package-lock.json src/renderer/src/components/Layout.tsx src/renderer/src/components/PdfPreview.tsx src/renderer/src/components/FileSidebar.tsx
git commit -m "feat: resizable panels (sidebar, preview, form)"
```

---

### Task 5: Auto-fit zoom au chargement

**Files:**
- Modify: `src/renderer/src/components/PdfPreview.tsx`

**Step 1:** Remplacer le state `scale` initial par un calcul auto-fit. Apres le chargement du PDF, calculer le scale optimal :

Dans `loadPdf` (useEffect de chargement), apres `const doc = await pdfjsLib.getDocument(...)`, ajouter :
```typescript
const firstPage = await doc.getPage(1)
const unscaledViewport = firstPage.getViewport({ scale: 1 })
const container = containerRef.current
if (container) {
  const containerWidth = container.clientWidth - 32 // padding
  const fitScale = containerWidth / unscaledViewport.width
  setScale(Math.max(0.5, Math.min(3, fitScale)))
}
```

**Step 2:** Garder le `useState(1.2)` comme fallback initial (avant le premier render).

**Step 3:** Run `npm run typecheck`

**Step 4:** Commit
```bash
git add src/renderer/src/components/PdfPreview.tsx
git commit -m "feat: auto-fit zoom to container width on PDF load"
```

---

### Task 6: Bouton stop IA + AbortController

**Files:**
- Modify: `src/main/services/aiService.ts`
- Modify: `src/main/ipc.ts`
- Modify: `src/preload/index.ts`
- Modify: `src/preload/index.d.ts`
- Modify: `src/renderer/src/components/ComptaForm.tsx`

**Step 1:** Dans `aiService.ts`, ajouter le support abort :
```typescript
let currentAbortController: AbortController | null = null

export function abortCurrentExtraction(): void {
  currentAbortController?.abort()
  currentAbortController = null
}
```

Dans `extractInvoiceData`, creer le controller et le passer au SDK :
```typescript
currentAbortController = new AbortController()
const response = await client.messages.create({
  model: 'claude-sonnet-4-20250514',
  max_tokens: 1024,
  messages: [...]
}, { signal: currentAbortController.signal })
```

Catcher `AbortError` dans le catch :
```typescript
if (err instanceof Error && err.name === 'AbortError') return null
```

**Step 2:** Dans `ipc.ts`, ajouter le handler :
```typescript
import { abortCurrentExtraction } from './services/aiService'
ipcMain.handle('ai-abort', async () => { abortCurrentExtraction(); return true })
```

**Step 3:** Dans `preload/index.ts` et `preload/index.d.ts`, ajouter :
```typescript
aiAbort: () => Promise<boolean>
// index.ts :
aiAbort: (): Promise<boolean> => ipcRenderer.invoke('ai-abort'),
```

**Step 4:** Dans `ComptaForm.tsx`, a cote du sparkle, ajouter un bouton stop visible quand `aiProcessing` :
```tsx
{aiProcessing && (
  <button
    type="button"
    className="p-1 rounded hover:bg-accent text-muted-foreground hover:text-destructive transition-colors cursor-pointer"
    onClick={async () => {
      await window.api.aiAbort()
      setAiProcessing(false)
    }}
    title="Arreter l'analyse IA"
  >
    <X className="h-4 w-4" />
  </button>
)}
```

**Step 5:** Run `npm run typecheck`

**Step 6:** Commit
```bash
git add src/main/services/aiService.ts src/main/ipc.ts src/preload/index.ts src/preload/index.d.ts src/renderer/src/components/ComptaForm.tsx
git commit -m "feat: abort IA button with AbortController support"
```

---

## Phase 3 -- Features moyennes (partiellement parallelisables)

### Task 7: Prefixer nom fichier par num compte (checkbox settings)

**Files:**
- Modify: `src/main/services/supplierMappingService.ts` (setting persiste)
- Modify: `src/main/ipc.ts` (handlers IPC)
- Modify: `src/preload/index.ts` + `src/preload/index.d.ts` (bridge + types)
- Modify: `src/renderer/src/components/SettingsPanel.tsx` (checkbox)
- Modify: `src/renderer/src/components/ComptaForm.tsx` (construction fileName)

**Step 1:** Pattern identique a `includeAmountInFilename` :
- `supplierMappingService.ts` : ajouter `prefixAccountInFilename: boolean` dans `AppConfig`, defaut `false`, getter/setter
- `ipc.ts` : handlers `get-prefix-account` / `set-prefix-account`
- `preload` : bridge + types

**Step 2:** Dans `SettingsPanel.tsx`, ajouter checkbox sous "Nommage des fichiers" :
```
Prefixer le nom du fichier par le numero de compte
```
Exemple : `601100 - Fournisseur - FAC-001.pdf`

**Step 3:** Dans `ComptaForm.tsx`, charger le setting et l'utiliser dans `handleValidate` pour construire `nameParts` :
```typescript
// En mode ventile avec prefix :
const accountPrefix = ventilationEnabled
  ? ventilation.map(v => v.accountNumber).join('+')
  : currentFormData.accountNumber
const nameParts = prefixAccount ? [accountPrefix, currentFormData.fixedPart, ...] : [currentFormData.fixedPart, ...]
```

**Step 4:** Run `npm run typecheck`

**Step 5:** Commit
```bash
git add src/main/services/supplierMappingService.ts src/main/ipc.ts src/preload/index.ts src/preload/index.d.ts src/renderer/src/components/SettingsPanel.tsx src/renderer/src/components/ComptaForm.tsx
git commit -m "feat: option to prefix filename with account number"
```

---

### Task 8: Modal gros fichier (seuil configurable)

**Files:**
- Modify: `src/main/services/supplierMappingService.ts` (setting seuil)
- Modify: `src/main/ipc.ts` (handler get-page-count + settings)
- Modify: `src/preload/index.ts` + `src/preload/index.d.ts`
- Modify: `src/renderer/src/components/SettingsPanel.tsx` (input seuil)
- Modify: `src/renderer/src/App.tsx` (modal avant chargement IA)

**Step 1:** Setting `largeFilePageThreshold: number` dans config, defaut 8. Getter/setter/IPC.

**Step 2:** Handler IPC `get-page-count` dans `ipc.ts` :
```typescript
ipcMain.handle('get-page-count', async (_event, filePath: string) => {
  const { PDFDocument } = await import('pdf-lib')
  const bytes = await readFile(filePath)
  const doc = await PDFDocument.load(bytes, { ignoreEncryption: true })
  return doc.getPageCount()
})
```

**Step 3:** Dans `App.tsx`, avant de lancer l'IA sur un nouveau PDF, verifier le nombre de pages. Si > seuil, afficher une modale de confirmation (state local `showLargeFileModal`). Si confirme, lancer l'IA. Si annule, ne pas lancer l'IA (le form reste vide, l'user saisit manuellement).

**Step 4:** Design de la modale : `max-w-sm`, icone warning amber, bouton confirm fuchsia sparkle, bouton annuler outline.

**Step 5:** Input seuil dans Settings > General : "Seuil d'avertissement gros fichier (pages)" avec un input number.

**Step 6:** Run `npm run typecheck`

**Step 7:** Commit
```bash
git add src/main/services/supplierMappingService.ts src/main/ipc.ts src/preload/index.ts src/preload/index.d.ts src/renderer/src/components/SettingsPanel.tsx src/renderer/src/App.tsx
git commit -m "feat: large file warning modal with configurable page threshold"
```

---

### Task 9: Spinner + reset form immediat + pre-render

**Files:**
- Modify: `src/renderer/src/store/useAppStore.ts` (ajout `fileLoading`)
- Modify: `src/renderer/src/components/PdfPreview.tsx` (overlay spinner)
- Modify: `src/renderer/src/App.tsx` (flux chargement)
- Modify: `src/renderer/src/components/FileSidebar.tsx` (reset au clic)

**Step 1:** Ajouter `fileLoading: boolean` + setter dans le store Zustand.

**Step 2:** Au changement de fichier (dans App.tsx, FileSidebar, nextFile, prevFile) :
1. `resetForm()` immediat (synchrone)
2. `setFileLoading(true)`
3. `ensurePdf()` en background
4. `setCurrentPdfPath()` + `setFileLoading(false)` quand pret
5. IA en parallele (aiProcessing gere separement)

**Step 3:** Dans `PdfPreview.tsx` ou `Layout.tsx`, afficher un overlay spinner quand `fileLoading` :
```tsx
{fileLoading && (
  <div className="absolute inset-0 z-10 flex items-center justify-center bg-background/70 backdrop-blur-[2px]">
    <Loader2 className="h-8 w-8 animate-spin text-muted-foreground" />
  </div>
)}
```

**Step 4:** Run `npm run typecheck`

**Step 5:** Commit
```bash
git add src/renderer/src/store/useAppStore.ts src/renderer/src/components/PdfPreview.tsx src/renderer/src/App.tsx src/renderer/src/components/FileSidebar.tsx
git commit -m "feat: instant form reset + loading spinner on file change"
```

---

## Phase 4 -- Feature complexe

### Task 10: Ignorer un document

**Files:**
- Modify: `src/renderer/src/store/useAppStore.ts` (ignoredFiles, removeWithoutDelete)
- Modify: `src/renderer/src/components/ComptaForm.tsx` (bouton Ignorer)
- Modify: `src/renderer/src/components/FileSidebar.tsx` (clic droit menu)
- Create: `src/renderer/src/components/ui/context-menu.tsx` (shadcn context menu)
- Modify: `src/renderer/src/App.tsx` (warning fermeture)
- Modify: `src/main/services/aiService.ts` (pre-classement 000000)
- Modify: `src/main/ipc.ts` (handler before-quit)

**Step 1:** Dans le store, ajouter :
```typescript
ignoredFiles: string[]  // paths des fichiers ignores
ignoreCurrentFile: () => void  // retire de la queue sans supprimer, ajoute a ignoredFiles
```

**Step 2:** Bouton "Ignorer" dans ComptaForm sous le bouton Valider :
```tsx
<Button
  variant="outline"
  className="w-full border-destructive/50 text-destructive hover:bg-destructive/10"
  onClick={handleIgnore}
  disabled={!currentPdfPath || isProcessing}
>
  Ignorer ce document
</Button>
```

**Step 3:** Clic droit dans FileSidebar : ajouter `onContextMenu` avec un menu shadcn/ui "Ignorer" sur chaque item.

**Step 4:** Warning fermeture app : dans `App.tsx`, ecouter `window.addEventListener('beforeunload')`. Si `ignoredFiles.length > 0`, afficher un confirm.

**Step 5:** IA pre-classement : dans le prompt d'extraction de `aiService.ts`, ajouter une regle :
```
Si le document ne semble pas etre une facture ou un ticket de caisse (rapport, releve, devis, plus de 4 pages, pas de montant clair), mets "000000" dans un champ "suggestedAccount" et "documents-divers" dans invoiceNumber.
```

Et dans le retour, mapper `suggestedAccount` vers `accountNumber` si present.

**Step 6:** Run `npm run typecheck`

**Step 7:** Commit
```bash
git add -A
git commit -m "feat: ignore document without delete, AI pre-classification, close warning"
```

---

## Parallelisation des tasks

```
Phase 1 (parallele) : Task 1 + Task 2 + Task 3
Phase 2 (sequentiel) : Task 4 -> Task 5 -> Task 6
Phase 3 (partiellement parallele) : Task 7 || Task 8, puis Task 9
Phase 4 : Task 10
```

Total : 10 tasks, ~10 commits, estimatoin 4 phases.
